#include "monty.h"

/**
 * stack - function to set data format to stack
 * @stack: variable to stack pointer
 * @line_number: counter for line where the function is called
 */

void stack(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
	arguments->stack = 1;
}
